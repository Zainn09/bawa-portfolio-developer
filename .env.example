# Set to your actual HTTPS origin when ready to publish. Unset previews are noindex.
NEXT_PUBLIC_SITE_URL=
# Server-only HTTPS endpoint that accepts JSON and reports successful delivery with 2xx.
CONTACT_WEBHOOK_URL=
# Optional server-side authentication for your trusted delivery service.
CONTACT_WEBHOOK_TOKEN=
